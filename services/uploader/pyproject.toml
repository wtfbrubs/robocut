[project]
name = "svc-uploader"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "clipbot-shared",
    "redis>=5.0",
    "httpx>=0.27",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
