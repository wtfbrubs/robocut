[project]
name = "svc-clip-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "clipbot-shared",
    "redis>=5.0",
    "ffmpeg-python>=0.2",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
